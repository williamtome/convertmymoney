<%- include('header') %>

<div class="messages">
  <% if(error){ %>
    <div class="error">
      <%= error %>
    </div>
  <% }else{ %>
    <div class="success">
      <%= quantidade %> dólares 
      na cotação: <%= cotacao %> 
      é igual á R$ <%= conversao %> reais.
    </div>
  <% } %>
  <a href="/">Voltar</a>
</div>

<%- include('footer') %>